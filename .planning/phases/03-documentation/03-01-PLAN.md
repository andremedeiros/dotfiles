---
phase: 03-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README contains installation instructions with prerequisites"
    - "README explains rcm and how dotfiles are managed"
    - "README documents LazyVim setup with first-launch instructions"
    - "README lists custom keybindings in a table format"
    - "README does not reference old vimscript config or deprecated plugins"
  artifacts:
    - path: "README.md"
      provides: "Complete dotfiles documentation"
      contains: "LazyVim"
      min_lines: 50
  key_links:
    - from: "README.md keybindings section"
      to: "config/nvim/lua/config/keymaps.lua"
      via: "documents same keybindings"
      pattern: "Leader.*p|H.*first non-whitespace"
---

<objective>
Update README.md with comprehensive documentation for the LazyVim migration.

Purpose: Complete the migration by documenting the new setup, replacing outdated vimscript-era documentation with current LazyVim information.

Output: Updated README.md with Installation, rcm explanation, Neovim/LazyVim setup, and custom keybindings sections.
</objective>

<execution_context>
@/Users/T992229/.claude/get-shit-done/workflows/execute-plan.md
@/Users/T992229/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation/03-CONTEXT.md
@.planning/phases/03-documentation/03-RESEARCH.md

# Source files for accurate documentation
@README.md
@config/nvim/lua/config/keymaps.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with LazyVim documentation</name>
  <files>README.md</files>
  <action>
Rewrite README.md with the following structure. Use patterns from RESEARCH.md.

**Structure:**
1. **Title and intro** - Brief description of dotfiles repo
2. **Installation** - Prerequisites (Homebrew, Xcode CLI), clone instructions, bootstrap command
3. **What is rcm?** - Brief explanation of rcm, how symlinks work, key commands (rcup, lsrc, rcdn), link to official docs
4. **Neovim** section with:
   - **Setup** - LazyVim explanation, first-launch instructions (plugins auto-install), checkhealth verification
   - **Custom Keybindings** - Table with `<kbd>` formatting documenting ONLY custom additions

**Custom keybindings to document (from keymaps.lua):**
| Key | Mode | Action |
|-----|------|--------|
| H | Normal | Go to first non-whitespace character |
| L | Normal | Go to end of line |
| // | Normal | Clear search highlight |
| < / > | Visual | Indent without losing selection |
| Leader + p | Normal | Find files |
| Leader + Tab | Normal | Switch to alternate buffer |
| Leader + z | Normal | Toggle window zoom |

Include note: "Leader key is `<kbd>Space</kbd>`. Press Space in normal mode to see all available commands via which-key."
Link to LazyVim keymaps docs for built-in bindings.

5. **Tmux** - Keep existing Tmux section (still relevant, no changes needed)
6. Remove or update any outdated sections (old NeoVim keybindings referring to deprecated plugins, Dash references, etc.)

**DO NOT:**
- Reference old vimscript config, init.vim, minpac, NERDTree, fzf.vim, vim-go, etc.
- Document LazyVim defaults (link to official docs instead)
- Create overly long documentation (target ~80-120 lines)

**Format keybindings using `<kbd>` HTML tags** for visual styling.
  </action>
  <verify>
1. `cat README.md | grep -c "LazyVim"` returns at least 2 (mentioned in setup)
2. `cat README.md | grep -c "<kbd>"` returns at least 15 (keybindings use kbd tags)
3. `cat README.md | grep -c "init.vim\|minpac\|NERDTree\|fzf.vim"` returns 0 (no old references)
4. `cat README.md | grep "rcm"` returns matches (rcm explained)
5. `wc -l < README.md` is between 70 and 150 (reasonable length)
  </verify>
  <done>
README.md updated with:
- Installation section with prerequisites
- rcm explanation with key commands
- Neovim/LazyVim setup instructions
- Custom keybindings table using <kbd> formatting
- No references to old vimscript config
- Existing Tmux section preserved
  </done>
</task>

</tasks>

<verification>
After completing the task:
1. Open README.md and verify all sections are present
2. Verify keybindings table matches config/nvim/lua/config/keymaps.lua
3. Search for any remaining references to old plugins (NERDTree, fzf.vim, minpac, vim-go)
4. Verify links to external docs work (rcm, LazyVim)
</verification>

<success_criteria>
Phase 3 requirements satisfied:
- [DOCS-01] README contains LazyVim setup instructions including rcm integration
- [DOCS-02] Old vimscript configuration removed (already done in prior phase, no new references added)
- [DOCS-03] Custom keybindings documented with descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation/03-01-SUMMARY.md`
</output>
