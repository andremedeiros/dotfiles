---
phase: 01-foundation-and-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Brewfile
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "tree-sitter CLI is available in PATH"
    - "hooks/post-up/03-vim-plugins validates tree-sitter without warning"
  artifacts:
    - path: "Brewfile"
      provides: "tree-sitter-cli formula for CLI tool"
      contains: "tree-sitter-cli"
  key_links:
    - from: "Brewfile"
      to: "hooks/post-up/03-vim-plugins"
      via: "brew bundle installs CLI, hook validates command"
      pattern: "tree-sitter-cli"
---

<objective>
Fix tree-sitter CLI dependency gap from Phase 01 verification.

Purpose: The Brewfile currently contains `tree-sitter` formula which only installs the library (libtree-sitter), not the CLI tool. The CLI is required for nvim-treesitter parser compilation and is validated by the post-up hook.
Output: Working tree-sitter CLI installation via Brewfile
</objective>

<execution_context>
@/Users/T992229/.claude/get-shit-done/workflows/execute-plan.md
@/Users/T992229/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-integration/01-VERIFICATION.md

# Gap being closed:
# - Truth: "All required dependencies verified (ripgrep, fd, Neovim >= 0.11.2)"
# - Status: partial - tree-sitter CLI not in PATH
# - Missing: tree-sitter CLI binary accessible via PATH

# Current state:
# - Brewfile line 24: brew 'tree-sitter' (library only, no CLI)
# - Homebrew has separate formula: tree-sitter-cli
# - Post-up hook at hooks/post-up/03-vim-plugins checks for `tree-sitter` command
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Brewfile to use tree-sitter-cli formula</name>
  <files>Brewfile</files>
  <action>
Replace the `tree-sitter` formula with `tree-sitter-cli` in the Brewfile.

Current line 24:
```
brew 'tree-sitter'   # Parser generator CLI (nvim-treesitter)
```

Change to:
```
brew 'tree-sitter-cli'   # Parser generator CLI (nvim-treesitter)
```

The `tree-sitter` formula only installs libtree-sitter (the library). The `tree-sitter-cli` formula installs the actual CLI binary that provides the `tree-sitter` command in PATH.

Note: tree-sitter-cli depends on tree-sitter library internally, so we don't need both formulas.
  </action>
  <verify>
Run: `grep tree-sitter Brewfile`
Expected: Line contains `tree-sitter-cli` (not just `tree-sitter`)
  </verify>
  <done>Brewfile contains `brew 'tree-sitter-cli'` formula</done>
</task>

<task type="auto">
  <name>Task 2: Install tree-sitter-cli and verify availability</name>
  <files>None (runtime verification)</files>
  <action>
1. Run `brew bundle --global` to install the updated Brewfile dependencies
2. Verify tree-sitter CLI is now available in PATH
3. Run the post-up hook to confirm it passes without warnings

Commands to run:
```bash
brew bundle --global
command -v tree-sitter
tree-sitter --version
./hooks/post-up/03-vim-plugins
```

All commands should succeed. The hook should show "OK: tree-sitter installed" instead of a warning.
  </action>
  <verify>
Run: `command -v tree-sitter && tree-sitter --version`
Expected: Path to tree-sitter binary and version >= 0.25.0

Run: `./hooks/post-up/03-vim-plugins 2>&1 | grep tree-sitter`
Expected: "OK: tree-sitter installed" (no WARN)
  </verify>
  <done>tree-sitter CLI is in PATH, hook validates without warning</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. `command -v tree-sitter` returns a path (e.g., /opt/homebrew/bin/tree-sitter)
2. `tree-sitter --version` shows version >= 0.25.0
3. `./hooks/post-up/03-vim-plugins` shows "OK: tree-sitter installed"
4. Requirements FOUND-04 and INTG-04 are now satisfied
</verification>

<success_criteria>
- tree-sitter CLI binary is available in PATH
- Brewfile uses correct formula (tree-sitter-cli)
- Post-up hook passes all dependency checks
- Gap from 01-VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-integration/01-03-SUMMARY.md`
</output>
