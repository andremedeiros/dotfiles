---
phase: 01-foundation-and-integration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - config/nvim/init.lua
  - config/nvim/lua/config/lazy.lua
  - config/nvim/lua/config/options.lua
  - config/nvim/lua/config/keymaps.lua
  - config/nvim/lua/config/autocmds.lua
  - config/nvim/lua/plugins/example.lua
  - config/nvim/.gitignore
  - hooks/post-up/03-vim-plugins
autonomous: false

must_haves:
  truths:
    - "Neovim launches with LazyVim UI (not old config)"
    - "lazy.nvim bootstraps automatically on first launch"
    - "Health checks pass for lazy.nvim and LazyVim"
    - "Config files are symlinked from dotfiles to ~/.config/nvim"
    - "Post-up hook validates Neovim installation"
  artifacts:
    - path: "config/nvim/init.lua"
      provides: "Neovim entry point with lazy.nvim bootstrap"
      contains: "LazyVim/LazyVim"
      min_lines: 20
    - path: "config/nvim/lua/config/lazy.lua"
      provides: "lazy.nvim configuration"
      contains: "require(\"lazy\").setup"
    - path: "config/nvim/lua/plugins/example.lua"
      provides: "Custom plugin specs placeholder"
      contains: "return"
    - path: "config/nvim/.gitignore"
      provides: "Ignore lazy-lock.json and data dirs"
      contains: "lazy-lock.json"
    - path: "hooks/post-up/03-vim-plugins"
      provides: "Neovim health check hook"
      contains: "nvim --version"
  key_links:
    - from: "config/nvim/init.lua"
      to: "config/nvim/lua/config/lazy.lua"
      via: "require statement"
      pattern: "require.*config.lazy"
    - from: "config/nvim/lua/config/lazy.lua"
      to: "LazyVim plugins"
      via: "lazy.nvim spec import"
      pattern: "import.*lazyvim.plugins"
    - from: "hooks/post-up/03-vim-plugins"
      to: "nvim binary"
      via: "version check"
      pattern: "nvim --version"
---

<objective>
Install LazyVim starter template and replace the old vim-plugins hook with a health check hook.

Purpose: Create a working LazyVim installation that integrates with rcm dotfiles management. The post-up hook validates the installation after rcup runs.

Output: Complete LazyVim config structure in config/nvim/, updated post-up hook for health validation.
</objective>

<execution_context>
@/Users/T992229/.claude/get-shit-done/workflows/execute-plan.md
@/Users/T992229/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-integration/01-CONTEXT.md
@.planning/phases/01-foundation-and-integration/01-RESEARCH.md
@.planning/phases/01-foundation-and-integration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LazyVim starter configuration</name>
  <files>
config/nvim/init.lua
config/nvim/lua/config/lazy.lua
config/nvim/lua/config/options.lua
config/nvim/lua/config/keymaps.lua
config/nvim/lua/config/autocmds.lua
config/nvim/lua/plugins/example.lua
config/nvim/.gitignore
  </files>
  <action>
Create the LazyVim starter template structure. Use the exact patterns from RESEARCH.md.

**1. Create `config/nvim/init.lua`:**
```lua
-- Bootstrap lazy.nvim
local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not (vim.uv or vim.loop).fs_stat(lazypath) then
  local lazyrepo = "https://github.com/folke/lazy.nvim.git"
  local out = vim.fn.system({ "git", "clone", "--filter=blob:none", "--branch=stable", lazyrepo, lazypath })
  if vim.v.shell_error ~= 0 then
    vim.api.nvim_echo({
      { "Failed to clone lazy.nvim:\n", "ErrorMsg" },
      { out, "WarningMsg" },
      { "\nPress any key to exit..." },
    }, true, {})
    vim.fn.getchar()
    os.exit(1)
  end
end
vim.opt.rtp:prepend(lazypath)

-- Make sure to setup `mapleader` and `maplocalleader` before
-- loading lazy.nvim so that mappings are correct.
vim.g.mapleader = " "
vim.g.maplocalleader = "\\"

-- Setup lazy.nvim
require("config.lazy")
```

**2. Create `config/nvim/lua/config/lazy.lua`:**
```lua
require("lazy").setup({
  spec = {
    -- Import LazyVim and its plugins
    { "LazyVim/LazyVim", import = "lazyvim.plugins" },
    -- Import/override with your plugins from lua/plugins
    { import = "plugins" },
  },
  defaults = {
    lazy = false,
    version = false,
  },
  install = { colorscheme = { "tokyonight", "habamax" } },
  checker = { enabled = true },
  performance = {
    rtp = {
      disabled_plugins = {
        "gzip",
        "tarPlugin",
        "tohtml",
        "tutor",
        "zipPlugin",
      },
    },
  },
})
```

**3. Create `config/nvim/lua/config/options.lua`:**
```lua
-- Options are automatically loaded before lazy.nvim startup
-- Default options that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/options.lua
-- Add any additional options here
```

**4. Create `config/nvim/lua/config/keymaps.lua`:**
```lua
-- Keymaps are automatically loaded on the VeryLazy event
-- Default keymaps that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/keymaps.lua
-- Add any additional keymaps here (Phase 2 will populate this)
```

**5. Create `config/nvim/lua/config/autocmds.lua`:**
```lua
-- Autocmds are automatically loaded on the VeryLazy event
-- Default autocmds that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/autocmds.lua
-- Add any additional autocmds here
```

**6. Create `config/nvim/lua/plugins/example.lua`:**
```lua
-- Every spec file under lua/plugins will be loaded automatically by lazy.nvim
-- Return an empty table for now; custom plugins will be added in Phase 2 if needed
return {}
```

**7. Create `config/nvim/.gitignore`:**
```gitignore
# Lazy.nvim lockfile (not tracking for personal dotfiles)
lazy-lock.json

# Data directories (should live in ~/.local/share/nvim/)
data/
plugin/
state/

# OS files
.DS_Store
```

Create necessary directories:
```bash
mkdir -p config/nvim/lua/config
mkdir -p config/nvim/lua/plugins
```
  </action>
  <verify>
Run: `find config/nvim -name "*.lua" | sort`
Expected output:
```
config/nvim/init.lua
config/nvim/lua/config/autocmds.lua
config/nvim/lua/config/keymaps.lua
config/nvim/lua/config/lazy.lua
config/nvim/lua/config/options.lua
config/nvim/lua/plugins/example.lua
```

Run: `grep "LazyVim/LazyVim" config/nvim/lua/config/lazy.lua`
Expected: Line containing the LazyVim import.

Run: `cat config/nvim/.gitignore | grep lazy-lock`
Expected: lazy-lock.json appears in .gitignore
  </verify>
  <done>LazyVim starter template structure created with all required files. lazy.nvim will bootstrap automatically on first Neovim launch.</done>
</task>

<task type="auto">
  <name>Task 2: Replace vim-plugins hook with health check</name>
  <files>hooks/post-up/03-vim-plugins</files>
  <action>
Replace the old `hooks/post-up/03-vim-plugins` hook (which installed minpac) with a new health check script that validates Neovim and its dependencies.

**Replace contents of `hooks/post-up/03-vim-plugins` with:**
```bash
#!/usr/bin/env bash
set -e

echo "==> Validating Neovim installation..."

# Check Neovim is available
if ! command -v nvim >/dev/null 2>&1; then
  echo "ERROR: Neovim not found in PATH"
  echo "Run: brew bundle --global"
  exit 1
fi

# Check version (LazyVim v15.x requires >= 0.11.2)
NVIM_VERSION=$(nvim --version | head -n1 | awk '{print $2}' | sed 's/v//')
REQUIRED_VERSION="0.11.2"

version_compare() {
  printf '%s\n' "$1" "$2" | sort -V | head -n1
}

if [ "$(version_compare "$REQUIRED_VERSION" "$NVIM_VERSION")" != "$REQUIRED_VERSION" ]; then
  echo "ERROR: Neovim $NVIM_VERSION is too old. Need >= $REQUIRED_VERSION"
  echo "Run: brew upgrade neovim"
  exit 1
fi

echo "OK: Neovim $NVIM_VERSION installed"

# Check LazyVim dependencies
DEPS_OK=true
for cmd in rg fd tree-sitter; do
  if command -v "$cmd" >/dev/null 2>&1; then
    echo "OK: $cmd installed"
  else
    echo "WARN: $cmd not found (required for LazyVim)"
    DEPS_OK=false
  fi
done

if [ "$DEPS_OK" = false ]; then
  echo ""
  echo "Missing dependencies. Run: brew bundle --global"
fi

echo ""
echo "==> LazyVim setup instructions:"
echo "    1. Run 'nvim' to bootstrap plugins on first launch"
echo "    2. Wait for plugin installation to complete"
echo "    3. Run ':checkhealth' to validate full installation"
```

Keep the same filename (`03-vim-plugins`) to maintain hook execution order. The name is historical but the functionality changes.
  </action>
  <verify>
Run: `head -5 hooks/post-up/03-vim-plugins`
Expected: Shows shebang and "Validating Neovim installation" message.

Run: `grep -c "minpac" hooks/post-up/03-vim-plugins`
Expected: 0 (no minpac references remain)

Run: `grep "nvim --version" hooks/post-up/03-vim-plugins`
Expected: Line containing version check.
  </verify>
  <done>Post-up hook validates Neovim version and LazyVim dependencies. No longer installs minpac.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify LazyVim launches correctly</name>
  <what-built>
Complete LazyVim starter configuration with:
- init.lua entry point with lazy.nvim bootstrap
- lua/config/ directory with options, keymaps, autocmds
- lua/plugins/ directory for custom plugins
- Post-up hook that validates dependencies
  </what-built>
  <how-to-verify>
1. From the dotfiles repo, run `rcup` to apply changes
2. Run `nvim` for the first time
3. Wait for lazy.nvim to bootstrap (should clone automatically)
4. Wait for plugins to install (progress shown in UI)
5. Once complete, run `:checkhealth lazy` - should show OK
6. Run `:checkhealth lazyvim` - should show OK
7. Run `:checkhealth nvim-treesitter` - should show OK (tree-sitter-cli found)
8. Verify LazyVim UI appears (dashboard, colorscheme, status line)

Expected:
- Neovim launches without errors
- Plugins install automatically
- Health checks pass (OK status for lazy, lazyvim, treesitter)
- TokyoNight colorscheme is active
  </how-to-verify>
  <resume-signal>Type "approved" if LazyVim works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `ls config/nvim/lua/config/` shows: autocmds.lua, keymaps.lua, lazy.lua, options.lua
2. `ls config/nvim/lua/plugins/` shows: example.lua
3. `grep "LazyVim/LazyVim" config/nvim/lua/config/lazy.lua` returns match
4. Post-up hook no longer references minpac
5. Human verification confirms LazyVim launches and health checks pass
</verification>

<success_criteria>
- LazyVim starter template complete in config/nvim/
- lazy.nvim bootstraps on first Neovim launch
- Plugins install automatically
- Health checks pass for lazy, lazyvim, nvim-treesitter
- Post-up hook validates dependencies
- Covers requirements: FOUND-01, FOUND-02, INTG-01, INTG-02, INTG-03, INTG-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-integration/01-02-SUMMARY.md`
</output>
