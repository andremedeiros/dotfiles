---
phase: 02-keybinding-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/nvim/lua/config/keymaps.lua
  - config/nvim/lua/plugins/yanky.lua
autonomous: false

must_haves:
  truths:
    - "H moves cursor to first non-whitespace character of line"
    - "L moves cursor to end of line"
    - "// clears search highlighting"
    - "Visual mode < indents left and keeps selection"
    - "Visual mode > indents right and keeps selection"
    - "Leader+p opens Telescope file finder"
    - "Leader+Tab switches to alternate buffer"
    - "Leader+z toggles window zoom"
    - "All custom bindings appear in which-key popup with descriptive labels"
  artifacts:
    - path: "config/nvim/lua/config/keymaps.lua"
      provides: "All custom keybindings with desc fields"
      contains: "vim.keymap.set"
    - path: "config/nvim/lua/plugins/yanky.lua"
      provides: "Disables yanky.nvim Leader+p binding"
      contains: '{ "<leader>p", false }'
  key_links:
    - from: "config/nvim/lua/config/keymaps.lua"
      to: "which-key discovery"
      via: "desc field in vim.keymap.set"
      pattern: 'desc = "'
    - from: "config/nvim/lua/config/keymaps.lua"
      to: "LazyVim.pick"
      via: "require for file finder"
      pattern: 'require\\("lazyvim.util"\\).pick'
    - from: "config/nvim/lua/config/keymaps.lua"
      to: "Snacks.zen.zoom"
      via: "require for window zoom"
      pattern: 'require\\("snacks"\\).zen.zoom'
---

<objective>
Implement all custom keybindings for Phase 2 of LazyVim migration: H/L for line navigation, // for clearing search, visual mode indent preservation, Leader+p for file finder, Leader+Tab for alternate buffer, and Leader+z for window zoom.

Purpose: Preserve muscle memory from old vimscript config while using LazyVim's modern plugin ecosystem (Telescope, Snacks). All bindings integrate with which-key for discoverability.

Output: Working keybindings in keymaps.lua, disabled yanky.nvim conflict, verified functionality.
</objective>

<execution_context>
@/Users/T992229/.claude/get-shit-done/workflows/execute-plan.md
@/Users/T992229/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-keybinding-migration/02-CONTEXT.md
@.planning/phases/02-keybinding-migration/02-RESEARCH.md
@config/nvim/lua/config/keymaps.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement all custom keybindings</name>
  <files>config/nvim/lua/config/keymaps.lua</files>
  <action>
Replace the placeholder keymaps.lua with full implementation containing:

1. **Navigation section** (H/L):
   - `H` in normal mode maps to `^` (first non-whitespace char)
   - `L` in normal mode maps to `$` (end of line)
   - Both with desc fields for which-key

2. **Search section** (//):
   - `//` in normal mode maps to `<cmd>noh<cr>` (clear highlight)
   - With desc field

3. **Editing section** (visual indent):
   - `<` in visual mode maps to `<gv` (indent left, reselect)
   - `>` in visual mode maps to `>gv` (indent right, reselect)
   - Both with desc fields

4. **Files section** (Leader+p):
   - `<leader>p` maps to `require("lazyvim.util").pick("files")()`
   - Use function wrapper for lazy loading
   - Desc: "Find files"

5. **Buffers section** (Leader+Tab):
   - `<leader><tab>` maps to `<cmd>buffer #<cr>`
   - Desc: "Alternate buffer"

6. **Windows section** (Leader+z):
   - `<leader>z` maps to `require("snacks").zen.zoom()`
   - Use function wrapper for lazy loading
   - Desc: "Toggle zoom"

Organize with clear section comments as shown in RESEARCH.md.
All bindings must include `desc` field for which-key integration.
  </action>
  <verify>
File exists and contains all 8 vim.keymap.set calls:
- grep for "H.*\\^" and "L.*\\$" (navigation)
- grep for "//" and "noh" (search)
- grep for "v.*<gv" and "v.*>gv" (visual indent)
- grep for "leader.*p" and "pick" (file finder)
- grep for "leader.*tab" and "buffer #" (alternate buffer)
- grep for "leader.*z" and "zoom" (window zoom)
  </verify>
  <done>keymaps.lua contains all 8 keybindings organized by category with desc fields</done>
</task>

<task type="auto">
  <name>Task 2: Disable yanky.nvim Leader+p conflict</name>
  <files>config/nvim/lua/plugins/yanky.lua</files>
  <action>
Create a new plugin spec file to disable yanky.nvim's default Leader+p binding (yank history) since we're using it for file finder.

Content:
```lua
-- Disable yanky.nvim's <leader>p binding (yank history)
-- We use <leader>p for file finder instead
return {
  "gbprod/yanky.nvim",
  keys = {
    { "<leader>p", false },
  },
}
```

This uses lazy.nvim's plugin spec keys table to disable the specific binding without affecting other yanky functionality.
  </action>
  <verify>
File exists at config/nvim/lua/plugins/yanky.lua and contains:
- "gbprod/yanky.nvim" string
- '{ "<leader>p", false }' pattern
  </verify>
  <done>yanky.lua plugin spec disables Leader+p conflict</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Custom keybindings for LazyVim: H/L line navigation, // search clear, visual indent, Leader+p file finder, Leader+Tab alternate buffer, Leader+z zoom</what-built>
  <how-to-verify>
1. Run `rcup` to apply dotfiles changes
2. Open Neovim in dotfiles directory: `cd ~/src/github.com/andremedeiros/dotfiles && nvim .`
3. Test each binding:

**Navigation (H/L):**
- Move to middle of a line with text
- Press `H` - cursor should jump to first non-whitespace character
- Press `L` - cursor should jump to end of line

**Search clear (//):**
- Search for something: `/vim<Enter>`
- Matches should be highlighted
- Press `//` - highlights should disappear

**Visual indent (<, >):**
- Select multiple lines with `V` and motion
- Press `>` - lines indent right, selection stays
- Press `<` - lines indent left, selection stays

**File finder (Leader+p):**
- Press `Space p` - Telescope file picker should open
- Search for a file, press Enter to open

**Alternate buffer (Leader+Tab):**
- Open a file, then open another file
- Press `Space Tab` - should switch to previous file
- Press `Space Tab` again - should switch back

**Window zoom (Leader+z):**
- Split window with `Ctrl-w v`
- Press `Space z` - current window should zoom to full
- Press `Space z` again - should restore split

**Which-key (all bindings visible):**
- Press `Space` and wait - which-key popup should show
- Verify "p" shows "Find files"
- Verify "z" shows "Toggle zoom"
- Verify "Tab" shows "Alternate buffer"

4. Confirm all 7 requirements work as expected
  </how-to-verify>
  <resume-signal>Type "approved" if all bindings work correctly, or describe which bindings have issues</resume-signal>
</task>

</tasks>

<verification>
All Phase 2 requirements (KEYS-01 through KEYS-07) verified working:
- [ ] KEYS-01: H/L mapped to line start/end
- [ ] KEYS-02: // mapped to clear search highlight
- [ ] KEYS-03: Visual mode < and > indent without losing selection
- [ ] KEYS-04: Leader+p mapped to file finder
- [ ] KEYS-05: Leader+Tab mapped to alternate buffer
- [ ] KEYS-06: Leader+z mapped to zoom window toggle
- [ ] KEYS-07: Custom keybindings show in which-key popup with labels
</verification>

<success_criteria>
- All 8 keybindings work as expected in Neovim
- Bindings override LazyVim defaults without errors
- All bindings appear in which-key popup with descriptive labels
- User approves after manual verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-keybinding-migration/02-01-SUMMARY.md`
</output>
