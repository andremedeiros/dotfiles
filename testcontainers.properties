# testcontainers-go reads this from ~/.testcontainers.properties.
#
# ryuk (the reaper container that cleans up test containers) must run
# privileged on podman: without this it starts and immediately dies.
# Verified empirically on a rootful podman machine (macOS, applehv).
ryuk.container.privileged=true
